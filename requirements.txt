# CPU-only torch. The default wheel pulls ~2.5 GB of CUDA libraries that are
# dead weight on a CPU Space; this index serves a ~200 MB build instead.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.26,<3
scipy>=1.11
soundfile>=0.12
pandas>=2.0
scikit-learn>=1.4
joblib>=1.3

pydantic>=2.12.5
pydantic-settings>=2.2

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# gradio 6 is required: gradio 5 caps pydantic below the version google-genai needs.
gradio>=6.0,<7

torch>=2.2
transformers>=5.0
safetensors>=0.4

google-genai>=1.0
groq>=0.11
faster-whisper>=1.0

# v2: PANNs CNN14 audio tagging, used as a second opinion for background_noise_type
# (app/audio/noise_panns.py). Measured +0.14 macro-F1 over the spectral-only
# classifier on the dev set — see eval/train_noise_panns.py. Weights are baked
# into the Dockerfile at build time (see infra note there); librosa is a
# panns_inference dependency, not used directly by this codebase.
panns_inference>=0.1
librosa>=0.10

# v2: emotion2vec+ (app/ser/emotion2vec_backend.py), a second, categorical SER
# opinion used narrowly in app/ser/mapping.py.reconcile() to corroborate the
# frustrated->upset boundary. Local model, no API key. Controlled by
# SER_ENSEMBLE_ENABLED (default on); degrades to "no signal" rather than
# failing the clip if these are absent.
funasr>=1.4
modelscope>=1.0

# Optional: pyannote overlap detection (app/audio/overlap.py). Only activates
# when OVERLAP_BACKEND=pyannote and HF_TOKEN are both set, and the HF_TOKEN's
# account must have accepted the licence at
# huggingface.co/pyannote/segmentation-3.0 once. Left commented out by default
# so a deployment that never sets those two env vars does not pay for the
# dependency.
# pyannote.audio>=4.0
